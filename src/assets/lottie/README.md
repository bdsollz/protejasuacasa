# Lottie placeholder

Este projeto usa `loader.json` com `lottie-web` como fallback funcional.

TODO para produção:
1. Substituir por um arquivo `.lottie` real em `src/assets/lottie/loader.lottie`.
2. Trocar o componente `LottieBadge` para usar `@lottiefiles/dotlottie-react` com autoplay/loop.
